name: ppe-training-pipeline

services:
  docs:
    container_name: docs
    build:
      context: .
      dockerfile: Dockerfile.docs
    ports:
      - "8000:8000"
    volumes:
      - ..:/docs
    command: serve --dev-addr=0.0.0.0:8000
